Upload this folder to a PHP-enabled host.

Important:
- The admin password hash is in api/admin/config.php
- The default password was generated for: cojina2025
- Change the hash after choosing a new password

Run locally with: php -S localhost:8000
Then open index.php in the browser.
